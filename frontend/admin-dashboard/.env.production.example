# Production frontend configuration template.
# If Nginx serves the frontend and proxies API requests on the same domain, leave this blank or omit it.
# If the API is served from a separate domain, set the full backend origin.

VITE_API_URL=
